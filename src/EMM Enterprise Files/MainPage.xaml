<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:EMM_Enterprise_Files" 
             x:Class="EMM_Enterprise_Files.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <!-- Fix: Set ItemsSource to a valid collection, e.g. Binding to a property -->
            <Label
                Text="Select a file to download"
                FontAttributes="Bold"
                HorizontalOptions="Center" 
                x:Name="InstructionLabel"/>
            <ListView ItemsSource="{x:Static local:EMMProfile.All}" RowHeight="120">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:IEMMProfile">
                        <ViewCell>
                            <Grid Padding="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="2*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Grid.RowSpan="2"
                           Source="{Binding Icon}"
                           Aspect="AspectFill"
                           HeightRequest="60"
                           WidthRequest="60" />
                                <Label Grid.Column="1"
                           Text="{Binding Name}"
                           FontAttributes="Bold" />
                                <Label Grid.Row="1"
                           Grid.Column="1"
                           Text="{Binding Path}"
                           FontAttributes="Italic"
                            />
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label
     Text="Status: Idle"
     FontAttributes="Bold"

     x:Name="StatusLabel"/>
            <ProgressBar Progress="0"
             ProgressColor="Orange"
             x:Name="DownloadProgressBar"
            />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>